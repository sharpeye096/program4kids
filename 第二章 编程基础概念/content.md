# 第二章：编程基础概念

## 2.1 变量：数据的容器

### 什么是变量？
变量就像是给数据起的名字标签🏷️，或者是一个小盒子📦，用来存放东西。

### 如何创建变量？
```python
# 给数字起名字
age = 10
score = 98.5

# 给文字起名字  
name = "小明"
school = "阳光小学"

# 给真假值起名字
is_student = True
likes_programming = False
```

### 变量命名规则：
1. **可以包含**：字母、数字、下划线 `_`
2. **不能以数字开头**：`1name` ❌，`name1` ✅
3. **区分大小写**：`age` 和 `Age` 是不同的变量
4. **不能使用关键字**：比如 `print`、`if` 等

### 使用变量：
```python
name = "小红"
age = 9
print(name + "今年" + str(age) + "岁了")
# 输出：小红今年9岁了
```

## 2.2 数据类型：数字、文字、真假值

### 数字类型
```python
# 整数 - 没有小数点的数字
age = 10          # int类型
class_size = 45      # int类型

# 浮点数 - 有小数点的数字  
height = 1.45        # float类型
weight = 35.5        # float类型
temperature = 36.8        # float类型
```

### 文字类型（字符串）
```python
# 用单引号或双引号包围
name = '小明'       # str类型
school = "希望小学"    # str类型
motto = '好好学习，天天向上'  # str类型

# 特殊字符
newline = "第一行\n第二行"  # \n表示换行
tab = "姓名\t年龄"    # \t表示制表符（Tab）
```

### 真假值类型（布尔值）
```python
is_sunny = True      # bool类型，表示真
is_raining = False     # bool类型，表示假

# 比较运算得到布尔值
result1 = 10 > 5     # True，10大于5
result2 = 3 == 4     # False，3不等于4
```

### 查看数据类型：
```python
print(type(10))        # <class 'int'>
print(type(3.14))      # <class 'float'>
print(type("hello"))   # <class 'str'>
print(type(True))      # <class 'bool'>
```

## 2.3 基本运算：加减乘除

### 算术运算
```python
# 加法
sum = 5 + 3        # 8

# 减法  
difference = 10 - 4       # 6

# 乘法
product = 6 * 7        # 42

# 除法
quotient = 15 / 3       # 5.0（注意结果是浮点数）

# 整除（去掉小数部分）
floor_division = 17 // 5   # 3

# 取余（求余数）
remainder = 17 % 5      # 2

# 幂运算
square = 5 ** 2      # 25
cube = 2 ** 3      # 8
```

### 字符串运算
```python
# 字符串拼接
greeting = "你好" + "！"          # "你好！"
full_name = "张" + "小明"          # "张小明"

# 字符串重复
stars = "⭐" * 5              # "⭐⭐⭐⭐⭐"
separator = "-" * 20            # "--------------------"

# 注意：数字和字符串不能直接相加
# print("年龄：" + 10)       # ❌ 错误！
print("年龄：" + str(10))    # ✅ 正确！
```

## 2.4 输入和输出：与程序对话

### 输出：print()函数
```python
# 基本输出
print("Hello World!")

# 输出多个值
print("姓名：", "小明", "年龄：", 10)

# 格式化输出
name = "小红"
age = 9
print(f"{name}今年{age}岁了")  # 小红今年9岁了

# 输出到一行（不换行）
print("正在加载", end="")
print("...", end="")
print("完成！")
# 输出：正在加载...完成！
```

### 输入：input()函数
```python
# 获取用户输入
name = input("请输入你的名字：")
age = input("请输入你的年龄：")

print(f"你好{name}，你{age}岁了！")

# 注意：input()返回的是字符串
# 如果需要数字，要转换类型
age_number = int(input("请输入年龄："))
next_year_age = age_number + 1
print(f"明年你就{next_year_age}岁了")
```

### 类型转换
```python
# 字符串转数字
number1 = int("10")      # 10（整数）
number2 = float("3.14")  # 3.14（浮点数）

# 数字转字符串
text1 = str(10)        # "10"
text2 = str(3.14)      # "3.14"

# 布尔值转换
result1 = bool(1)        # True
result2 = bool(0)        # False
result3 = bool("hello")  # True
result4 = bool("")       # False
```

## 2.5 小练习

### 练习1：个人信息收集
```python
# 收集用户信息并显示
name = input("你叫什么名字？")
age = int(input("你几岁了？"))
school = input("你在哪个学校上学？")

print("\n=== 个人信息 ===")
print(f"姓名：{name}")
print(f"年龄：{age}")
print(f"学校：{school}")
print("=" * 20)
```

### 练习2：数学小助手
```python
# 帮助计算数学题
number1 = float(input("请输入第一个数字："))
number2 = float(input("请输入第二个数字："))

print(f"\n计算结果：")
print(f"{number1} + {number2} = {number1 + number2}")
print(f"{number1} - {number2} = {number1 - number2}")
print(f"{number1} × {number2} = {number1 * number2}")
print(f"{number1} ÷ {number2} = {number1 / number2}")
```

### 练习3：创意作品
```python
# 用变量和输出创造有趣的内容
animal = input("你最喜欢的动物是什么？")
color = input("你最喜欢的颜色是什么？")
number = int(input("你最喜欢的数字是什么？"))

print("\n✨ 你的创意作品：")
print(animal * number)
print(f"这是一只{color}的{animal}！")
print("✨" * (number + 2))
```

## 2.6 本章小结

🎯 **学到了什么？**
- 变量是数据的容器，可以存储各种信息
- Python有4种基本数据类型：整数、浮点数、字符串、布尔值
- 可以进行算术运算和字符串操作
- 使用input()获取用户输入，print()显示输出
- 学会了类型转换：str()、int()、float()

🚀 **下一步准备**
在下一章，我们将学习控制程序流程，让程序能够做决定和重复执行任务！

---
💡 **编程小技巧**：
给变量起有意义的名字，比如用`age`而不是`a`，这样代码更容易理解！